# Finance Assistant Bot

A basic Node.js bot that runs on Render.

## Features
- Simple Express server
- Ready to deploy with GitHub + Render

## Deploy
1. Push to GitHub
2. Connect to Render.com
3. Use `render.yaml` for auto-deploy
